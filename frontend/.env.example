# Copy to .env to override the backend URL (defaults to http://localhost:8000)
VITE_API_BASE_URL=http://localhost:8000

# Optional: API key for SaaS mode (when the backend has AUTH_REQUIRED=true).
# Leave unset for local dev — the backend runs as the default tenant.
# VITE_API_KEY=clc_your-key-here
